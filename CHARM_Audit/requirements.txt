pandas>=2.0
numpy>=1.24
lxml>=4.9

scikit-learn>=1.4
lightgbm>=4.3
statsmodels>=0.14
joblib>=1.3

# Phase 2 deep model (gnn.py). torch_geometric requires a matching
# torch wheel; on Apple Silicon use the official torch wheel and the
# torch_geometric pure-python wheel (no torch_scatter / torch_sparse
# unless you specifically want CUDA-accelerated samplers).
torch>=2.2
torch-geometric>=2.5

xarray>=2024.1
netCDF4>=1.6
h5netcdf>=1.3
h5py>=3.10
dask>=2024.1
erddapy>=2.2
pyarrow>=15.0

# NASA Earthdata (PACE / MODIS / VIIRS)
earthaccess>=0.10
s3fs>=2024.6

# AWS / S3-backed Data/ cache (storage.py)
boto3>=1.34
universal_pathlib>=0.2

# ECMWF CDS (ERA5)
cdsapi>=0.7.7

# Copernicus Marine (GLORYS12)
copernicusmarine>=2.0

matplotlib>=3.8
